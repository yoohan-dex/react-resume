import React from 'react';
import Header from './Header';
const Index = (props) => (
  <div className="container">
    <main>
      <Header {...props} />
    </main>
  </div>
);

export default Index;
